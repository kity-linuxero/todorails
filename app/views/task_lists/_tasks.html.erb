<h4 style="float: left;">Tasks</h4>
<div class="table-responsive">
<table class="table table-striped table-sm ">
  <thead class="thead-inverse">
    <tr>
      <th>Description</th>
      <th>Status</th>
      <th>Priority</th>
      <th>Type</th>
      <th>Completed%</th>
      <th>Start at</th>
      <th>End at</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @task_list.tasks.sort.each do |task| %>
      <tr>
        <td><%= task.description %></td>
        <td><%= task.status_task %></td>
        <td><%= task.priority %></td>
        <td><%= task.type %></td>
        <td align= "center"><%= task.percentage_of_completion_task %></td>
        <td><%= task.start_at_task %></td>
        <td><%= task.end_at_task %></td>
        <td><%= link_to 'Details', task %></td>
        <td><%= link_to 'Edit', edit_task_path(task, @task_list) %></td>
        <td><%= link_to 'Destroy', task, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
</div>

<br>
